[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Documentación hw-monitor"
authors = [{name = "chsegura", email = "chseguraguzman@gmail.com"}]
dynamic = ["version", "description"]
